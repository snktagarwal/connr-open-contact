#This file contains the sockets for the python flup process, which are
#fastcgi applications
#TODO: make names meaningful
upstream connr_sockets{
	
	server unix:/tmp/connr_service-1.socket;
	server unix:/tmp/connr_service-2.socket;
}
